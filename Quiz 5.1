import pandas as py
import numpy as np

exam_data = {'name': ['Anastasia', 'Dima', 'Katherine', 'James', 'Emily', 'Michael', 'Matthew', 'Laura', 'Kevin', 'Jonas'],
'score': [12.5, 9, 16.5, np.nan, 9, 20, 14.5, np.nan, 8, 19],
'attempts': [1, 3, 2, 3, 2, 3, 1, 1, 2, 1],
'qualify': ['yes', 'no', 'yes', 'no', 'no', 'yes', 'yes', 'no', 'no', 'yes']}
labels = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j']

count = py.DataFrame(exam_data , index=labels)
print("Number of rows and columns:")
print(count.shape)

Output:
Number of rows and columns:
(10, 4)

Explanation:

I implemented the pandas and numpy modules and created the initiatior code. Starting with count calling the Dataframe of pandas. then simply by utilizing the .shape function I printed the count.shape to show the rows and columns
